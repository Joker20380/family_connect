# Исправленный TCP-helper: установка и host smoke — 11.09.2026

После [120/120 изолированных проб на новой сети](2026-09-11-network-change.ru.md)
выполнена ограниченная проверка на ноутбуке. Исходники helper из экспериментальной
ветки pilot/tcp-reality-2026-09-11, routing fix 6d7f569.

## Установка

Проверены отсутствие активного TCP/парного WG/AWG, marker и конфликтов routing rules/table.
Установленные backend.py/profile_config.py побайтно совпали с зависимостями эксперимента.
Старый root-owned helper сохранён в /var/backups/family-connect/tcp-routing-1789119289/helper
(0600). Новый helper атомарно установлен в /usr/local/lib/family-connect-tcp/helper.
SHA256: a4eafb70cedbfdd4ba77fd2c636ba4fd40c9f43c5a9ba62c5c1037bd1ff8a232.
Профиль, ключи, systemd unit и Xray не заменялись. Сервер не изменялся.

Независимый systemd timer fc-tcp-routing-smoke-guard ограничивал тест остановкой
TCP-службы через 75 секунд. После успешной очистки timer остановлен. При ошибке
тест предусматривает возврат старого helper после подтверждения очистки; откат не понадобился.

## Фактически проверено

- Три штатные probe_interface проверки HTTPS с ожидаемым egress IP прошли.
  Эта функция имеет два резервирующих endpoint: число не означает три запроса к каждому.
- resolvectl status содержит DNS 1.1.1.1 и домен ~.; query example.com через интерфейс прошёл.
- Три более точных локальных маршрута сохранили прежний dev; проверялся выбор маршрута,
  а не соединение с каждым LAN/Docker-сервисом.
- Маршрут к VPN-серверу проходит вне TUN, публичный 1.1.1.1 — через TUN.
- После systemctl stop интерфейс и ownership marker отсутствуют;
  полные IPv4/IPv6 policy rules совпали с сохранённым состоянием до теста.

[Машиночитаемый результат](../tcp-routing-host-result.json). Скрипт завершился exit 0.
Python syntax check пройден. Приложение и UI не менялись, полный Python/GTK-набор
повторно не запускался; прежние 222 passed не выдаются за новую проверку.

## Текущее состояние и дальнейшая приёмка

Новый helper остаётся установленным; тестовый TCP-туннель остановлен и очищен.
Стабильный Linux 0.2.7, опубликованный desktop 0.2.8, сервер 0.2.1 и Xray 26.3.27
остаются прежними. Основной WG/AWG launcher не заменён TCP-экспериментом.
Нет push, CI, релиза или установки экспериментального приложения пользователю.

**Следующий шаг — established AWG → TCP recovery**: установить рабочую AWG-сессию,
контролируемо сделать UDP endpoint недоступным, проверить мониторинг/переключение
и возврат DNS/маршрутов при остановке. Использовать ограниченный сценарий с watchdog
и собственными точными firewall selectors. Этот сценарий ещё не выполнен в данном этапе;
короткий smoke не заменяет проверку восстановления и длительной устойчивости.

Для отката: остановить family-connect-tcp@fctcp75850269.service, подтвердить отсутствие
TUN/marker/правил 10627–10630, затем атомарно восстановить backup helper как root:root 0755.
Не восстанавливать старый helper поверх активного нового marker/routing состояния.
Ключи, профили и остальные компоненты при откате не менять.
