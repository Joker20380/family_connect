# Windows TCP: установщик и интерфейс — 2026-09-11

Реализован следующий пункт этапа 4: закреплённые Xray/Wintun включены в установщик,
выбор WireGuard/TCP preview, отдельный импорт .fctcpactivation, подключение TCP без
WG-активации, отмена pending и отображение асинхронных ошибок службы. Статус on
по-прежнему не доказывает доступность интернета. Автоматического reconnect нет.

Сборка проверяет оба SHA256 против уже принятого engine; установочная проверка
повторяет сравнение после Inno Setup и проверяет наличие лицензий/манифеста.
Удаление проверяет отсутствие TCP executable. UI CI расширен до 288 геометрий
(RU/EN, WG/TCP, 6 состояний, 3 размера, 4 масштаба), проверяет TCP-only активацию,
отмену pending и ошибку при неизменном состоянии. Реальных сетевых вызовов UI нет.

Проверки: git diff --check выполнен. Первый platform CI34641386305 отказал при проверке SHA256: WireGuard build оставлял
в окружении свой Go1.26.2-zx2c4 вместо Go1.26.1 для Xray. Установщик не был создан.
Исправление сохраняет/восстанавливает переменные toolchain вокруг WG build; Xray
дополнительно отказывает до сборки при неверном Go. Закреплённые хеши не менялись.
Повторный CI исходника **34a3211** прошёл:
- [Client builds 34641891335](https://github.com/Joker20380/family_connect/actions/runs/34641891335): Windows/Linux/Android success; release skipped.
- [TCP native/session 34641891317](https://github.com/Joker20380/family_connect/actions/runs/34641891317): success, прежние lifecycle18/18, session24/24 HTTP, 4 DNS и 5 очисток сохранены.
- [phase0 34641891414](https://github.com/Joker20380/family_connect/actions/runs/34641891414): tests/failover success.

Windows runtime: установка, хеши обоих TCP binaries/лицензии, драйвер, broker activation,
18 TCP storage checks, UI smoke, 288 макетов/регрессии и удаление прошли.
Архив установщика скачан, SHA256SUMS перепроверен; SHA256 установщика: `f16a7ab01b3ec1fcd4550a84b04c90c03a64c8911472bdcb2b3126609fafb4bc`.
CI-снимок окна просмотрен. [Машинный результат](2026-09-11-windows-tcp-ui-result.json).


Версии: source/installer 0.2.9 только CI preview; опубликованный 0.2.9 не заменяется.
Нового релиза, подписи каталога, установки на устройства и изменений сервера нет.
Linux установлен 0.2.7, Windows последний подтверждённый 0.2.7, сервер 0.2.1,
TCP component/Setup 0.1.0. Android обновление и пользовательские сетевые тесты отложены.

Распространение: сначала отдельная приёмка внешнего REALITY/полной маршрутизации,
затем новая версия, platform CI, проверка скачанных бинарников и подпись каталога
офлайн. Возврат: отключить TCP и установить предыдущий проверенный установщик;
защищённые профили сохраняются. При сбое очистки сначала восстановить службу по
[runbook](../windows-tcp-session.ru.md), не удалять журнал вручную.

Далее Windows recovery/full-routing acceptance и AWG, затем Android AWG/TCP,
после интеграции платформ — этап 5 Reticulum. Магазин приложений не является барьером.
