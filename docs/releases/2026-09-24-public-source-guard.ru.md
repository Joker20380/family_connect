# Проверка публикуемых файлов —24.09.2026

Добавлен `scripts/check_public_sources.py`: проверка Git index, опасных путей,
известных credentials и приватных ключей без вывода значений. Подключён в phase0 CI
и обязательный порядок CONTRIBUTING. [Правила и ограничения](../secrets-and-recovery.ru.md).
Два теста прошли: распознавание/узкие fixture exceptions и настоящий временный Git
index, отличающийся от рабочего дерева. Интеграционный тест пропускается только
в окружении без Git (например минимальном Docker image).

Проверка дерева выявила только два старых all-zero WG fixtures; разрешены только
нулевые значения в этих точных test paths. Реальные секреты не читались/переносились.
CI-проверка выполняется после push и не заменяет локальную проверку перед отправкой.

Проверены GitHub Actions предыдущих публикаций: для86eb24e прошли phase0, Messenger,
Desktop visual, Windows ordinary user, Linux control preview, Windows control
conformance; Client builds, Windows AWG/TCP ещё выполнялись. Для0b2f641 phase0 passed.
Новые изменения требуют своего CI; завершение всех platform jobs не заявляется.

Рабочее место: есть GPG; съёмного диска нет. Выбранные пользователем зашифрованное
хранилище на компьютере и офлайн-копия ещё не настроены. Для дальнейшей настройки
нужен отдельный носитель и ввод пароля самим владельцем вне чата. Без этого перенос
ключей не выполнялся. Android service/UI/native acceptance также остаётся открытым;
этот шаг посвящён безопасной публикации и не меняет VPN runtime.

Версии Android beta50/code50, Linux0.2.10, Windows0.2.13 прежние. Откат guard — revert
изменений скрипта/workflow; данные и схемы хранилищ не изменены. Публикация изменений
в main выполняется после локальных проверок; результат фиксируется ответом Git push.
