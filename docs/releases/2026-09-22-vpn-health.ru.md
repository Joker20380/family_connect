# Проверка VPN и числа клиентов — 2026-09-22

Read-only SSH-замер около 16:16–16:18 UTC (18:16–18:18 Europe/Brussels).
Основной Friends-сервис: 3 активных устройства на NL, 0 на RU по handshake
за последние 5 минут; окно 15 минут даёт тот же результат. На NL все 3 peer
передавали данные в дополнительном 3-секундном замере. TCP established sockets
на Friends-портах: 0 на обоих серверах. Это снимок устройств, не уникальных людей.

SQLite mode=ro: 13 активированных устройств, 9 не отозвано, 4 отозвано;
65 записей приглашений, 13 использованных, 7 referral claims. Это разные счётчики.
AWG и TCP содержат по 8 настроенных peers/users на каждом сервере; их нельзя
суммировать как уникальных клиентов. Согласованность отзыва с gateway отдельно
не проверялась. Последний handshake за 24 часа имеют 1 peer RU и 5 NL;
это не полная история сессий и не дедуплицированное число пользователей.

Friends AWG/TCP, API RU и мониторинг работают. NRestarts: NL AWG=1
(текущий запуск с 17 сентября), остальные проверенные Friends AWG/TCP=0.
Причина исторического рестарта не исследовалась. HTTPS мониторинг: 200,
TLS проверен стандартным urllib. CPU RU 13.8–20.7%, NL 2.6–5.5%;
сетевой трафик в снимках менее 0.05 Мбит/с на направление.
RAM available RU1124/1962 MiB, NL622/955 MiB. Диск RU65%, NL18%.
200 Мбит/с в мониторинге остаётся оценкой, не подтверждённым тарифом.

Старые интерфейсы: RU pilot WG (3 peers) и AWG (1), NL fcams (4)
и fcawg31 (1) — без handshake за 5 минут. Запрос wg0 в старом RU
family-connect-data-gateway-wg-1 неуспешен; его активность не установлена.
Старые TCP pilots не подсчитывались. Число 3 относится к Friends-сервису.
Docker-контейнеры Family Connect RU работают, product API/control healthy.
Docker на NL отсутствует, там используются systemd-службы.

Последний документированный выпуск: Android0.1.18-beta35/code35,
Linux0.2.9 bundle146d221d0ad23c07, Windows0.2.9 preview a6c68fe.
Артефакты/установки заново не сверялись; новых сборок и rollout нет.
Откат не требуется: серверные файлы, службы и профили не менялись.
Незакоммиченная работа сохранена. Проверки: агрегаты SQLite, systemd,
handshake/transfer, TCP sockets, ресурсы и HTTPS; тесты кода не запускались.

Остаются отдельными задачами: длительная устойчивость и сквозная клиентская
проверка маршрутизации, полный аудит старых pilots, сверка отозванных доступов,
подтверждение тарифов и план мощности. План на 50 одновременных клиентов
не является измерением текущего числа подключений.
