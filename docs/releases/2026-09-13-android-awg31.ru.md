# Android AWG3.1 — 13.09.2026

Пользователь запросил AWG3.1 после неудачного звонка WhatsApp. Сервер подтвердил
WG handshake Android peer в15:52:22UTC, но сайты/DNS на телефоне ещё не проверены.
Нельзя приписывать сбой звонка доказанной блокировке всех других транспортов.

## Изменения

Engine b5928efb6ca19f0153958460c3d141f04abc5c2e (v3.1.20260828) с ранее
проверенным pilot/awg31 startup S4 patch. Android wrapper остаётся5420011143f9dd42831cc95fcdb0d6ac9bde868f,
расширяется явным patch; module path JNI и fixture переведён на /v3.
Ручной AWG профиль принимает HeaderProtectionKey (ненулевой32-byte Base64),
ContentPaddingAddition0..256/range, RandomTrailers и DisableCookies true/false.
При HPK требуются S1..S4>=12 и H1..H4=1,2,3,4; trailers требуют равные S.
Старые AWG2 профили остаются валидными; WG отвергает AWG поля. Таймеры3.1 пока
не входят в поддерживаемый набор. Native signed control schema по-прежнему
отвергает3.1: её исторические vectors не ослабляются ради ручного пилота.
[Официальное описание параметров](https://docs.amnezia.org/documentation/amnezia-wg/).

VersionName0.1.1-beta02/code2; UI показывает AmneziaWG3.1. APK ещё не выдан.
Эмуляторные peers и Android fixtures теперь используют одинаковый тестовый HPK,
padding/trailers; ключ03*32 — только синтетический, не production.

## Проверки и незавершённое

Начальная локальная Gradle попытка не загрузила native services из старого кэша.
Повтор с новым изолированным кэшем собрал Java;29/30 JVM tests passed. Новый
roundtrip test упёрся в существующее требование точной строки ::/0 при том, что
wrapper сериализует expanded IPv6. Roundtrip проверяется напрямую wrapper parser;
production profile validation не ослаблялась. Финальный повтор выполняется.
Нативная4ABI сборка, Android emulator runtime, Amsterdam AWG deployment/smoke,
подпись beta-ключом и проверка на физическом телефоне ещё не завершены.

Rollback: beta01 APK и WG peer сохранены. Android source до7fceed9 остаётся
предыдущей принятой версией. Новые серверные изменения пока не применены.

## Итоговая приёмка beta02

Android CI run34768083248/job103752559759 **success**, исходникd0709bc.
30 JVM tests и5 Android instrumentation tests passed, без failures/skips; native
4ABI hashes, лицензии, отсутствие fixtures проверены. Linux/Windows jobs также
success. Локально JVM30+lint passed; Python485 passed/2 existing warnings/9.48s.
Первоначальная проверка equals для Config также выявила identity-based KeyPair
equality в upstream wrapper; roundtrip проверен по точному UAPI, включая HPK.

[Принятая сборка](https://github.com/Joker20380/family_connect/actions/runs/34768083248).
Artifact10320104767, архив93970394bytes, SHA256
89a42575d9fd6435c183e8e2a21304d51ec6b3b6f2ce37f555932def5fa2ee99.
Публичный nightly.link download сверён с digest официального GitHub API.
Исходная CI подписьv2 проверена. Из универсального APK извлечён ARM64 payload
скриптом package-arm64.py: Java/DEX/resources/native library сохранены побайтно,
удалены остальные ABI и старые подписи, assets/awg-build.json сужен доARM64 и
содержит hash исходного APK. Выполнены zipalign16KiB и подпись сохранённым beta-key.
Это упаковка принятого кода, не новая приёмка на физическомARM64 телефоне.

Итоговый `FamilyConnect-Android-0.1.1-beta02-arm64.apk`: **51154127bytes**, SHA256
**2de9d0a1e1c509a99e1dea14341c32ee4a391edbc38ea0779d6464434b9399b8**.
Подписиv2/v3 и alignment проверены; сертификат
67a90d1bfcd5a2c0666f0cff1b0ac5e43aaa661ca1196f89e879aa39fe20848a совпадает сbeta01.
Packagecom.familyconnect.app, version0.1.1-beta02/code2, minSdk26, ARM64, debuggable.

## Амстердам

Отдельная службаfamily-connect-awg31.service active+enabled, интерфейсfcawg31,
UDP443, подсеть10.80.0.0/24; peer10.80.0.2/fd80:92::2. Публичные ключи и метрики
в JSON receipt. Бинарники с ранее принятого experiment2, hashes закреплены в
awg31_install.py. Private server config0600 и HPK не включены вGit/CI/отчёт.
Первый старт был остановлен: tools требуют on/off, а Go APItrue/false. Исправлены
булевы поля только нового server.conf, затем служба запущена и профиль сохранён.
Android profile используетtrue/false; отдельный Linux tool config — on/off.

Проверены gateway ping3/3, DNS, два HTTPSegress186.246.45.246/NL,1MiB download,
private-network/IPv6 block, client reconnect+HTTPS. Namespace/interface/IPC удалены,
host routes/rules/resolver сохранены. Это реальный внешний сервер с тестовым
Linux-клиентом, не проверка российской сети или WhatsApp. Server restart в этом
тесте не выполнялся. После проверки WG, AWG3.1 и Reticulum active; новая служба
NRestarts0, память~4.2MiB в одном замере, не нагрузочный/пиковый показатель.
Старые WG peers/relay/control journal не заменялись.

## Выдача и откат

APK/receipt/instruction сохраняются в ignored `state-client-build/android-pilots/beta02/`.
Приватный профиль — `state-enroll/android-amsterdam-awg31-beta02/`.
[Инструкция первого телефона](../testing/android-beta02-awg31.ru.md).
Следующий gate: обновление beta01→beta02, ручной AWG3.1, IP/HTTPS, сообщения и
звонки WhatsApp в российской сети, затем sleep/handover. Отказ Auto не считается
устранённым этим ручным профилем; полноценный набор профилей/onboarding остаётся задачей.

Серверный откат: `systemctl disable --now family-connect-awg31.service` останавливает
только новый пилот; ExecStopPost удаляет его интерфейс и две собственные nft tables.
Приватные config/keys сохраняются для контролируемого восстановления; WG/RNS
не затрагиваются. На Android достаточно Disconnect и выбора прежнего WireGuard.
Не удалять приложение ради downgrade: code1 нижеcode2, локальные профили потеряются.
