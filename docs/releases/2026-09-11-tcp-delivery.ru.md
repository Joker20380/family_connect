# TCP: подпись каталога и проверенная установка — 11.09.2026

Добавлены scripts/sign_tcp.py и scripts/tcp_delivery.py: отдельный домен Ed25519,
закреплённый публичный app anchor, отдельные component version/sequence, HTTPS загрузка,
размер/hash/срок, persistent anti-rollback, повторная root-проверка и закрытая распаковка
перед существующим установщиком. UI и шестифайловый app archive не менялись.
[Формат, команды, доверие, выпуск и откат](../tcp-delivery.ru.md).

## Проверки

Полный suite: **250 passed**, два прежних deprecation warnings. 20 новых проверок:
чужой ключ/домен, срок/будущее, типы/URL/размер/component/architecture, лишние поля;
перезапуск и rollback/conflict/immutable version, повреждение/переполнение загрузки,
path traversal/symlink/duplicate, предел распаковки, root reverify и приватный снимок,
ошибка installer без ложного installed marker.

Настоящий архив SHA256
860c8e0fbf4808e5235e9649c3d6e53f00e7ad4d81a36146f70c7ea144e48494
подписан одноразовым ключом в памяти. В одноразовом Debian 12 systemd-контейнере
проверены отказ изменённому архиву до установки, реальная root установка,
исполнение Xray и loaded systemd unit без автозапуска VPN. После добавления предела
распаковки повторная установка прошла. При первом повторе операторский harness
остановился на требовании отсутствия уже установленного helper; изменён только harness
для повторной установки, дефекта доставки это не выявило. Контейнер удалён.
Xray 26.3.27 и байты комплекта не менялись. Свежая установка с итоговым пределом
распаковки отдельно не повторялась; итоговый путь прошёл переустановку и unit tests.

HTTPS transport в unit tests подставлен; публичного TCP release/catalog ещё нет.
Проверка в контейнере использовала явный test anchor через Python API; CLI по умолчанию
доверяет только anchor репозитория. Production key не читался/не использовался.
Проверяющий код запускался из доверенного checkout: bootstrap/root-owned broker для
массовой установки ещё предстоит. Reticulum доставка не реализована этим шагом.

## Состояние и дальше

На ноутбук ничего не установлено, server 0.2.1, stable Linux 0.2.7, desktop release 0.2.8
сохранены. Remote CI/push/release/catalog не выполнялись. Следующий шаг — CI итоговой
ревизии, загрузка и проверка артефакта, затем первый отдельный подписанный TCP pilot
release/catalog и настоящий HTTPS smoke. Далее broker/UI, Windows/Android и VM gate
перед широким rollout. Старое неуточнённое health/recovery наблюдение остаётся открытым.
