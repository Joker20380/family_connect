# Desktop AWG 3.1 — 2026-09-20

Рабочий этап, не опубликованный выпуск. База desktop c20c16c, root main3f6f662
с сохранёнными несвязанными изменениями. Предыдущий Windows/Linux Client35490043368:
platform jobs success, aggregate failure из-за Android setup. Linux control35490344947
success, phase0 наc20c16c failure. Последний public desktopv0.2.9,
Android0.1.18-beta19/code19; установленные приложения и серверы не изменялись.

Engine b5928efb6ca19f0153958460c3d141f04abc5c2e, tools
ee0f0a9aa34ff0a0da4b3433b9512781cfe02843, Go1.26.1. Сохраняются startup и u16 patches.
Windows legacy activation schema остаётся прежней; Friends использует отдельный
runtime объект. Полный выданный IPv4 сохраняется в journal v2; v1 читается для старых
сессий. Native Windows пока поддерживает DNS1.1.1.1/MTU1280; другие подписанные
значения отклоняются явно. Linux принимает новый профиль только с установленным
pinned bundle marker; установщик сверяет hashes до изменения файлов.

Проверки:43 targeted Python;36 Windows catalog vectors и существующий core suite
(non-Windows DPAPI skipped); Windows Go worker/fixture cross-build; Linux image
fb96756beac2 и реальный изолированный handshake/HTTP с10.78.42.254/32 passed.
Первый тест выявил несовпадение true/false vs on/off в tools; helper преобразует
проверенный профиль в native формат и обратно при чтении. Windows runtime,
GTK, helper acceptance и полный regression pending.

Rollout: только новая immutable версия после platform CI/render/interaction и
проверки скачанных assets; затем offline signing scripts/sign_update.py с возрастающим
sequence. Не заменятьv0.2.9. Linux31 bundle получать изpilot/awg31/Dockerfile;
install-awg.sh проверяет hashes. Шесть файлов standalone archive сохраняются.
Rollback: остановить VPN и выполнить cleanup новым helper/broker прежде чем вернуть
проверенные прежние бинарники; v1 broker не читает pending journalv2, старые Linux
tools не читают31. Не удалять identity/cache ради отката. Server rollout отсутствует.

Открыто: platform CI, live Friends Linux/Windows, signed catalog/installer выпуск.

## Уточнение и проверка полного пути

Пользователь сообщил, что AWG2 уже не работает в России. Российский рабочий путь:
AWG3.1/TCP REALITY; backward compatibility test не доказывает доступность из РФ.
На69129ea Windows native прошёл реальные engine AWG2 и AWG3.1 rounds, но installer
и broker ещё содержали прежний accepted hash. Исправлено676b011: новый worker
SHA256 e3d11b9552eb8ed84776cf16a8c240e90b4b9ff0d361519c840909ca5f97fdb6.
Linux helper lifecycle и wrong-key cleanup passed; полный Python629 passed;
GTK Friends transport/parent passed, рендер просмотрен. Windows core35491752787
success; Linux AWG35491998529 success. Ожидание Windows финальных CI продолжается.
Добавлена отдельная нативная CI проверка подписанный Friends catalog → runtime →
адрес10.83.42.254/32 → journalv2 → crash recovery; ещё pending. Test-only broker
harness не входит в production executable; его endpoint замещён localhost только
при TCP_SESSION_TEST, маршруты остаются узкими тестовыми.
