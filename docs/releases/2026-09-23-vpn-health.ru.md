# Проверка VPN — 2026-09-23

Read-only SSH, 10:13–10:17 UTC / 12:13–12:17 Europe/Brussels.
Friends: NL AWG 4 peer с handshake ≤5 минут (и ≤15 минут), RU 0;
из них 3 передавали данные за отдельные 5 секунд. TCP established на Friends
портах NL443/RU8446: 0. Это устройства, не уникальные люди.
SQLite mode=ro: 16 активированных устройств, 12 не отозвано, 4 отозвано;
69 приглашений, 16 использовано, 11 referral claims. Со вчерашнего отчёта
+3 активации, +3 действующих устройства, +4 referral claims.
Настроено AWG peers RU9/NL11; не суммировать как уникальных пользователей.

| Метрика | RU | NL |
|---|---:|---:|
| CPU, отдельные короткие замеры | 8.9–20.9% | 1.0–9.3% |
| RAM available/total MiB | 1143/1962 | 624/955 |
| Диск занят | 64.9% | 18.3% |
| Свободно на диске GiB | 9.8 | 14.9 |
| RX/TX Мбит/с, 5 секунд | 0.012/0.001 | 0.084/0.071 |
| RX/TX Мбит/с, последующий мониторинг | 0.017/0.008 | 3.409/3.581 |
| Uptime сервера, дни | 21.9 | 10.0 |

Это отдельные снимки, не диапазоны за сутки. Capacity200 Мбит/с в мониторинге
остаётся provider-default-estimate; процент доступного канала не подтверждён.
На RU занято около270 MiB swap, при наличии1143 MiB доступной RAM; признаков
OOM за сутки нет. На NL swap отсутствует.

Friends AWG/TCP, RU access API и мониторинг active; NRestarts=0 в текущих
запусках. На NL отдельного access API нет по архитектуре. Failed units на
обоих хостах отсутствуют. Family Connect Docker-контейнеры RU работают,
product API и control healthy. HTTPS /status/server-load.json вернул200,
проверка TLS стандартным urllib успешна; оба gateway присутствуют.

## Ошибки и перезапуски за 24 часа

RU: 03:11:51 UTC остановка AWG, через20 секунд stop-sigterm timeout,
SIGKILL python3/amneziawg-go и Failed with result timeout; в03:12:11 UTC
служба запущена снова. По Брюсселю05:11:51–05:12:11.
NL: 03:19:27–03:19:32 UTC штатная остановка/запуск AWG (05:19 местного
времени Брюсселя). Мониторинг перезапущен на обоих, access API на RU.
Перезапуски совпадают с unattended-upgrade libexpat1 и других системных
пакетов; это вероятная причина по сопоставлению времени, инициатор отдельно
не доказан. Сами хосты не перезагружались. В этой проверке службы не меняли.

В остальных проверенных Friends systemd-журналах error/fail/exception/
traceback/timeout/panic/fatal не найдены; TCP-журналы пусты за24 часа.
Kernel: не найдены OOM, segfault, I/O error, nf_conntrack table full.
Docker logs HTTPS за24 часа пуст: это пробел наблюдаемости, не доказательство
отсутствия HTTP4xx/5xx. Сквозной клиентский трафик/маршрутизация не проверялись.

Счётчики handshake/transfer сброшены сегодняшним перезапуском AWG:
NL2.975 GiB суммарного RX+TX с текущих peer, RU0 в первом снимке;
это не суточный объём и не полная история посещений. Поэтому DAU/WAU и
уникальных людей по текущим данным достоверно посчитать нельзя.
Старые pilots и согласованность отзыва gateway не проверялись.

Последние документированные версии: Android0.1.18-beta35/code35,
Linux0.2.9 bundle146d221d0ad23c07, Windows0.2.9 preview a6c68fe.
Артефакты/установки повторно не проверены. Rollout/rollback отсутствуют:
серверные файлы, конфигурация и службы не изменены. Тесты кода не запускались.

Далее: исследовать завершение RU AWG по SIGTERM; обеспечить агрегаты
активности/ошибок с историей, проверить существующую конфигурацию HTTP logging;
сохранить прежние release gates, аудит отзыва и план мощности как открытые задачи.
